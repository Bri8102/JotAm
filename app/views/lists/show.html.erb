<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @list.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @list.description %>
</p>

<div id="tasks_wrapper">
 <%= render @list.tasks %>
</div>


<%= link_to 'Edit', edit_list_path(@list) %> | <%= link_to 'Delete', @list, method: :delete, data: { confirm: 'Are you sure?' } %> | <%= link_to "Create Task", new_list_task_path(@list) %> |